spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
        use_sql_comments: true
        format_sql: true

  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/collector_camel_poc


scheduler-routes:
  collector:
    id: CollectorSchedulerRoute
    cron-expression: 0/5 * * ? * * *
    thread-pool: 10